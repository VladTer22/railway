<tr>
  <td><%= carriage.type %></td>
  <td><%= carriage.number %></td>
  <td><%= carriage.up_seats  || "0"%></td>
  <td><%= carriage.down_seats || "0" %></td>
  <td><%= carriage.side_seats_top || "0" %></td>
  <td><%= carriage.side_seats_bottom || "0" %></td>
  <td><%= carriage.sitting_seats || "0" %></td>
  <td><%= carriage.trains_ids.number || "0" %></td>
  <td><%= link_to 'Show', carriage %></td>
  <td><%= link_to 'Edit', edit_carriage_path(carriage) %></td>
  <td><%= link_to 'Destroy', carriage, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
